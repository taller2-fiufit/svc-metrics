apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  labels:
    app: svc-metrics
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc-metrics
  template:
    metadata:
      labels:
        app: svc-metrics
    spec:
      containers:
        - name: backend
          image: okteto.dev/svc-metrics
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: env-config
          env:
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: secrets
                  key: JWT_SECRET
          ports:
            - containerPort: 3000
          #readinessProbe:
           # httpGet:
            #  path: /health
             # port: 3000

---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: svc-metrics
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
